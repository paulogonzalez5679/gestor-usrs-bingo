// Environment configuration for Gestor Usrs Bingo
// Usar la variable de entorno REACT_APP_API_URL si está definida (create-react-app / Vite env vars).
// Fallback a la URL previa para desarrollo rápido.
export const API_BASE_URL = (typeof process !== 'undefined' && process.env && process.env.REACT_APP_API_URL)
	? process.env.REACT_APP_API_URL
	: 'http://127.0.0.1:5000';
// Otra posible URL de túnel: https://wellness-vary-healthcare-sleeve.trycloudflare.com